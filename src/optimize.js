import * as T from 'three';
import {mergeGeometries} from 'three/addons/utils/BufferGeometryUtils.js';
// Consolida malhas estáticas por material dentro de cada camada reversível.
export function optimize(group){group.updateWorldMatrix(true,true);const inverse=group.matrixWorld.clone().invert(),batches=new Map();group.traverse(o=>{if(!o.isMesh||Array.isArray(o.material))return;let g=o.geometry.clone();g.applyMatrix4(new T.Matrix4().multiplyMatrices(inverse,o.matrixWorld));if(g.index)g=g.toNonIndexed();if(!g.attributes.uv)g.setAttribute('uv',new T.Float32BufferAttribute(new Float32Array(g.attributes.position.count*2),2));const entry=batches.get(o.material)||[];entry.push(g);batches.set(o.material,entry);});group.clear();for(const [material,geometries]of batches){const merged=mergeGeometries(geometries,false);if(!merged)throw Error('Géométrie incompatible');const mesh=new T.Mesh(merged,material);mesh.name=group.name;mesh.castShadow=true;mesh.receiveShadow=true;group.add(mesh);for(const g of geometries)g.dispose();}}
